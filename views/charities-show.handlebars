<a href="/">Back to Charities</a><div class="pull-right">
  <a href="/charities/{{charity._id}}/users/new" class="btn btn-primary">Add People</a>
</div>
<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <h1>{{charity.name}}</h1>
        <h3>Cause : {{charity.cause}}</h3>
        <p>{{charity.description}}</p>

        <p><a href="/charities/{{charity._id}}/edit">Edit</a></p>
        <p>
            <form method="POST" action="/charities/{{charity._id}}?_method=DELETE">
                <button class="btn btn-primary" type="submit">Delete</button>
            </form>
        </p>
    </div>
</div>

 <!-- Show Users -->
 <div class="pull-left">
   <h2>Participants<h2>

     {{#each users}}
      {{> user}}
     {{/each}}

 </div>
